// src/auto-mix.js — safe stub (under 300 lines). Real mixer comes later.
let enabled = true;
export function startAutoMix(){ /* no-op for now; reserved for future */ }
export function setAutoMixEnabled(v){ enabled = !!v; }
export function isAutoMixEnabled(){ return enabled; }
